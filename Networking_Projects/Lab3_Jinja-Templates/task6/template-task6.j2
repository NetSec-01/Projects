{% for interface in interface_list %}
 interface {{ interface.name }}
 description {{ interface.desc }}
 {% if interface["uplink"]%} switch port Trunk
 {% else %} switchport mode access
  switchport access vlan {{interface["vlan"]}}
 {% endif %}
{% endfor %}
